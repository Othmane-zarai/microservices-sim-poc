# scenario_seed: 245522987
apiVersion: v1
kind: Pod
metadata:
  name: rq1-s113
  labels:
    rq1-scenario: "s113"
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      resources:
        requests: {cpu: 50m, memory: 32Mi}
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
            - key: zone
              operator: In
              values: ["c"]
  tolerations:
    - key: dedicated
      operator: Equal
      value: gpu
      effect: NoSchedule
