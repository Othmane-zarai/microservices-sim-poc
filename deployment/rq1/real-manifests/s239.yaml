# scenario_seed: 3236151037
apiVersion: v1
kind: Pod
metadata:
  name: rq1-s239
  labels:
    rq1-scenario: "s239"
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      resources:
        requests: {cpu: 50m, memory: 32Mi}
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
            - key: rack
              operator: In
              values: ["r1"]
