# scenario_seed: 3882343658
apiVersion: v1
kind: Pod
metadata:
  name: rq1-s150
  labels:
    rq1-scenario: "s150"
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      resources:
        requests: {cpu: 50m, memory: 32Mi}
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
            - key: role
              operator: In
              values: ["gpu"]
  tolerations:
    - key: dedicated
      operator: Equal
      value: gpu
      effect: NoSchedule
