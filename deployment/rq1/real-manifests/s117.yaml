# scenario_seed: 2555656321
apiVersion: v1
kind: Pod
metadata:
  name: rq1-s117
  labels:
    rq1-scenario: "s117"
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      resources:
        requests: {cpu: 50m, memory: 32Mi}
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
            - key: rack
              operator: In
              values: ["r2"]
