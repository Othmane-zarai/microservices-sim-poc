# RQ1 Matched-Scoring Experiment (LEAST_ALLOCATED) — Result

Generated by: comparison-scripts/rq1_matched_scoring.py over deployment/rq1/rq1-la-fresh/
(300 fresh LEAST_ALLOCATED runs via run-rq1-fast.ps1, patched build with the
taint-toleration bonus removed). The kss baseline is the substrate-aligned run
(bg-web-a/bg-locust seeded on worker-1/worker-2 so kube-scheduler sees the same
loaded cluster the simulator does; see comparison-scripts/rq1_kss_compare.py
--seed-bg-pods).

```
scenarios with kss baseline = 300; LA logs parsed = 300
[sanity] CO sim vs kss   = 248/300 = 82.7%
[result] LA sim vs kss   = 248/300 = 82.7%
[result] placements changed CO->LA = 0/300
[result] LA sim vs real  strict = 199/300 = 66.3%  combined(score-set) = 300/300 = 100.0%
```

Interpretation: switching the simulator from COST_OPTIMIZED (best-fit) to a
matched LEAST_ALLOCATED (spread) profile changes none of the 300 placements, so
the residual strict-winner gap is **not** the best-fit-vs-spread scoring choice.
On the substrate-aligned cluster the kss winner falls inside the simulator's
tied-minimum-score set for all 300 scenarios (combined = 100%), so the residual
is a deterministic-vs-random **tie-break** difference among co-optimal nodes,
not a score-phase divergence.
